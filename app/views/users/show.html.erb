<% provide(:title, @user.name)%>
<% provide(:title, @user.email)%>

<div class="row">
  <aside class="col-md-8">
    <section class="user_info">
      <h1>
<% if current_user.admin? || current_user==@user %>
<li><%= link_to "View History", :action => :checkout, :controller => "checkout_logs", :id => @user.id %></li>
<% end %>
  <li><%= @user.name %></li>
  <li><%= @user.email %></li>

</h1>
    </section>
  </aside>
</div>
